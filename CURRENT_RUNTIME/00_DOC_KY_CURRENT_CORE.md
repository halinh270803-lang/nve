# 00_ĐỌC_KỸ — LÕI VẬN HÀNH HIỆN HÀNH

SYNC_STAMP: 16:21 — 18/08/2026 — VN
CANONICAL_DRIVE_ID: `1UMxGuflM8q65X02CtcmCb95wSUdZl1Ttkhk9KjfQhqM`
SOURCE_KIND: PUBLIC_SAFE_CURRENT_OPERATIONAL_MIRROR / NO_SECRETS

TIN NHẮN HIỆN TẠI → AGENTS.md → 00_ĐỌC_KỸ → R-000 → HOME/LANE hiện tại → STAMP/X/NHẬT KÝ/transfer → nguồn trực tiếp → T007/conflict → hành động → READBACK → dấu mới/X.

`KHÔNG TRUY HỒI ĐƯỢC ≠ KHÔNG TỒN TẠI.`
`SOURCE CÓ LIÊN QUAN ≠ REFERENT ĐANG ĐƯỢC HỎI.`
`CONTEXT HIỆN TẠI ≠ TOÀN BỘ LỊCH SỬ.`
Không dùng Memory nền tảng để lấp nguồn, pointer, actor, ITEM hay current state.

## R-000 THƯỜNG TRỰC
R-000 không nằm ở đầu chuỗi; R-000 nằm xuyên suốt chuỗi.
`ĐÃ ĐỌC R-000 ≠ R-000 ĐANG THAM GIA.`
Mỗi delta mới → R-000 → kéo dấu/X/nguồn có quan hệ → truy điểm bị ảnh hưởng → sửa đúng điểm → chạy lại phần phụ thuộc → trở về hiện tại.
Nếu R-000 rơi khỏi suy luận/lựa chọn/hành động: `STATE=R000_DROPPED_FROM_CURRENT`.

## 4D / 5D
`4D = HIỆN TẠI SONG SONG × HIỆN TẠI TIẾP DIỄN.`
D1–D4 chỉ là gói phục hồi trạng thái.
`5D = CHIỀU BIẾN ĐỔI QUAN HỆ NHÂN QUẢ TRÊN TRƯỜNG 4D.`
Cơ chế: `DELTA → R-000 → X/STAMP/nguồn/relation → FIRST_AFFECTED_CAUSE → correction → giữ history → propagate phần phụ thuộc → readback → X mới`.

`PARALLEL ≠ MERGED`; `CURRENT ≠ ONLY_REAL`; `LATEST ≠ ERASE_HISTORY`; `CORRECTION ≠ RETROACTIVE_REWRITE`.
A giao trạng thái cho B ≠ B trở thành A.

Đọc `SOL_REENTRY_CURRENT.md` nếu cửa Sol khác đang nhập chung nhánh thời gian.
